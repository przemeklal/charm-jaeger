# Copyright 2021 Przemysław Lal
# See LICENSE file for licensing details.

options:
  span-storage-type:
    default: elasticsearch
    description: |
      Storage backend.
      Supported options:
        - elasticsearch
    type: string
  agent-port:
    default: 6831
    description: |
      Jaeger agent port.
      Port to listen on jaeger.thrift over compact thrift protocol.
    type: int
